services:
  myportfolio:
    build: .
    restart: always
    env_file: .env
    ports:
      - "5000:5000"
    depends_on:
      - mysql

  mysql:
    image: mariadb:latest
    restart: always
    env_file: .env
    volumes:
      - mydatabase:/var/lib/mysql

volumes:
  mydatabase:
